import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'baltika-seller',
  webDir: 'dist/baltika-seller',
  bundledWebRuntime: false
};

export default config;
